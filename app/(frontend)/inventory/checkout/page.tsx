import { CheckoutStation } from './CheckoutStation'

export const metadata = {
  title: 'Checkout Station | CSME Concordia',
  description: 'CSME lab checkout station. Scan your student card and items to check out equipment.',
}

export default function CheckoutPage() {
  return <CheckoutStation />
}
